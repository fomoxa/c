// GENERATED BY fomoxac
// DO NOT EDIT MANUALLY
// source: src/models/game.h
// model: PlayerInput
// codec: input
// fingerprint: sha256:0a2dc2c0a2f0630eb4e8944bd6e084fe4f33ed022128ea371f625027b996e2d8
// fomoxac-version: 0.2.0
// generated-at: 2026-08-23T14:44:47Z

#pragma once

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>
#include <stdlib.h>
#include <string.h>

#include "runtime.h"
#include "src/models/game.h"
#include "vector3_input.h"

// The "input" codec for PlayerInput, generated from its Fomoxa markers.
//
// The wire layout, in declaration order (RFC-0002 SS5.1):
//
//  0. `tick`: `u64`
//  1. `direction`: `Vector3`
//  2. `firing`: `bool`

static const char *const PlayerInputInputCodec_MESSAGE_NAME = "PlayerInput.input";
static const uint32_t PlayerInputInputCodec_MESSAGE_ID = 0xA3177FFCu;
static const uint64_t PlayerInputInputCodec_FINGERPRINT = 0x0A2DC2C0A2F0630EULL;

// Writes the "input" fields of `*value`, in declaration order. Returns `false` (buffer
// untouched beyond what was already written) if growing the writer's buffer failed.
static inline bool PlayerInputInputCodec_encode(FomoxaWriter *writer, const struct PlayerInput *value) {
    if (!fomoxa_writer_write_u64(writer, value->tick)) return false;
    if (!Vector3InputCodec_encode(writer, &value->direction)) return false;
    if (!fomoxa_writer_write_bool(writer, value->firing)) return false;
    return true;
}

// Reads the "input" fields into `*value`, in declaration order.
//
// Fields this codec does not carry are left as they were, which is what lets one
// model be split across several codecs.
//
// A field the stream ended before takes its zero value (RFC-0002 SS9.1); a field
// the stream ended inside is an error. Bytes left over after the last field
// belong to a newer writer's model and are ignored.
//
// `*value` must not already hold data from a previous, unfreed decode - see
// runtime.h's module docs.
static inline FomoxaDecodeError PlayerInputInputCodec_decode(FomoxaReader *reader, struct PlayerInput *value) {
    if (fomoxa_reader_field_absent(reader)) {
        value->tick = 0;
    } else {
        FomoxaDecodeError error = fomoxa_reader_read_u64(reader, &value->tick);
        if (!fomoxa_decode_error_ok(&error)) return error;
    }
    {
        FomoxaDecodeError error = Vector3InputCodec_decode(reader, &value->direction);
        if (!fomoxa_decode_error_ok(&error)) return error;
    }
    if (fomoxa_reader_field_absent(reader)) {
        value->firing = false;
    } else {
        FomoxaDecodeError error = fomoxa_reader_read_bool(reader, &value->firing);
        if (!fomoxa_decode_error_ok(&error)) return error;
    }
    return fomoxa_decode_ok();
}
