// GENERATED BY cyclonec
// DO NOT EDIT MANUALLY
// source: src/models/game.h
// model: PlayerInput
// codec: input
// fingerprint: sha256:9b57ff586837d6541abe689c04bce84075d2e2d29b10113c8376b1db5e82fb25
// cyclonec-version: 0.2.0
// generated-at: 2026-08-20T13:35:44Z

#pragma once

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>
#include <stdlib.h>
#include <string.h>

#include "runtime.h"
#include "src/models/game.h"
#include "vector3_input.h"

// The "input" codec for PlayerInput, generated from its Cyclone markers.
//
// The wire layout, in declaration order (RFC-0002 SS5.1):
//
//  0. `tick`: `u64`
//  1. `direction`: `Vector3`
//  2. `firing`: `bool`

static const char *const PlayerInputInputCodec_MESSAGE_NAME = "PlayerInput.input";
static const uint32_t PlayerInputInputCodec_MESSAGE_ID = 0x74FDFA74u;
static const uint64_t PlayerInputInputCodec_FINGERPRINT = 0x9B57FF586837D654ULL;

// Writes the "input" fields of `*value`, in declaration order. Returns `false` (buffer
// untouched beyond what was already written) if growing the writer's buffer failed.
static inline bool PlayerInputInputCodec_encode(CycloneWriter *writer, const struct PlayerInput *value) {
    if (!cyclone_writer_write_u64(writer, value->tick)) return false;
    if (!Vector3InputCodec_encode(writer, &value->direction)) return false;
    if (!cyclone_writer_write_bool(writer, value->firing)) return false;
    return true;
}

// Reads the "input" fields into `*value`, in declaration order.
//
// Fields this codec does not carry are left as they were, which is what lets one
// model be split across several codecs.
//
// A field the stream ended before takes its zero value (RFC-0002 SS9.1); a field
// the stream ended inside is an error. Bytes left over after the last field
// belong to a newer writer's model and are ignored.
//
// `*value` must not already hold data from a previous, unfreed decode - see
// runtime.h's module docs.
static inline CycloneDecodeError PlayerInputInputCodec_decode(CycloneReader *reader, struct PlayerInput *value) {
    if (cyclone_reader_field_absent(reader)) {
        value->tick = 0;
    } else {
        CycloneDecodeError error = cyclone_reader_read_u64(reader, &value->tick);
        if (!cyclone_decode_error_ok(&error)) return error;
    }
    {
        CycloneDecodeError error = Vector3InputCodec_decode(reader, &value->direction);
        if (!cyclone_decode_error_ok(&error)) return error;
    }
    if (cyclone_reader_field_absent(reader)) {
        value->firing = false;
    } else {
        CycloneDecodeError error = cyclone_reader_read_bool(reader, &value->firing);
        if (!cyclone_decode_error_ok(&error)) return error;
    }
    return cyclone_decode_ok();
}
