// GENERATED BY cyclonec
// DO NOT EDIT MANUALLY
// source: src/models/game.h
// model: Vector3
// codec: input
// fingerprint: sha256:b6f55f6b96e4c39d87c6c1afcc003d4ba19c7ac41a1b44ce31a63235a102edd4
// cyclonec-version: 0.2.0
// generated-at: 2026-08-20T13:35:44Z

#pragma once

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>
#include <stdlib.h>
#include <string.h>

#include "runtime.h"
#include "src/models/game.h"

// The "input" codec for Vector3, generated from its Cyclone markers.
//
// The wire layout, in declaration order (RFC-0002 SS5.1):
//
//  0. `x`: `f32`
//  1. `y`: `f32`
//  2. `z`: `f32`

static const char *const Vector3InputCodec_MESSAGE_NAME = "Vector3.input";
static const uint32_t Vector3InputCodec_MESSAGE_ID = 0x6AA0B45Cu;
static const uint64_t Vector3InputCodec_FINGERPRINT = 0xB6F55F6B96E4C39DULL;

// Writes the "input" fields of `*value`, in declaration order. Returns `false` (buffer
// untouched beyond what was already written) if growing the writer's buffer failed.
static inline bool Vector3InputCodec_encode(CycloneWriter *writer, const struct Vector3 *value) {
    if (!cyclone_writer_write_f32(writer, value->x)) return false;
    if (!cyclone_writer_write_f32(writer, value->y)) return false;
    if (!cyclone_writer_write_f32(writer, value->z)) return false;
    return true;
}

// Reads the "input" fields into `*value`, in declaration order.
//
// Fields this codec does not carry are left as they were, which is what lets one
// model be split across several codecs.
//
// A field the stream ended before takes its zero value (RFC-0002 SS9.1); a field
// the stream ended inside is an error. Bytes left over after the last field
// belong to a newer writer's model and are ignored.
//
// `*value` must not already hold data from a previous, unfreed decode - see
// runtime.h's module docs.
static inline CycloneDecodeError Vector3InputCodec_decode(CycloneReader *reader, struct Vector3 *value) {
    if (cyclone_reader_field_absent(reader)) {
        value->x = 0.0f;
    } else {
        CycloneDecodeError error = cyclone_reader_read_f32(reader, &value->x);
        if (!cyclone_decode_error_ok(&error)) return error;
    }
    if (cyclone_reader_field_absent(reader)) {
        value->y = 0.0f;
    } else {
        CycloneDecodeError error = cyclone_reader_read_f32(reader, &value->y);
        if (!cyclone_decode_error_ok(&error)) return error;
    }
    if (cyclone_reader_field_absent(reader)) {
        value->z = 0.0f;
    } else {
        CycloneDecodeError error = cyclone_reader_read_f32(reader, &value->z);
        if (!cyclone_decode_error_ok(&error)) return error;
    }
    return cyclone_decode_ok();
}
