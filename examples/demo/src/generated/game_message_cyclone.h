// GENERATED BY cyclonec
// DO NOT EDIT MANUALLY
// source: src/models/game.h
// model: GameMessage
// fingerprint: sha256:30a6a572f1717682e74d6dae0f0add57135119a73874c0e5ba043e2b94562dcb
// cyclonec-version: 0.2.0
// generated-at: 2026-08-20T13:35:44Z

#pragma once

#include <stddef.h>
#include <stdlib.h>

#include "runtime.h"
#include "src/models/game.h"
#include "vector3_cyclone.h"

// Releases every heap allocation a decoded struct GameMessage owns: every `string`, `bytes`
// and `Array<T>` field, and every nested model field, recursively - whichever
// of GameMessage's codecs actually populated them.
//
// Safe on a freshly zero-initialized struct GameMessage (struct GameMessage value = {0};), on one this
// model's decode functions have populated, or on one already freed - never on
// one still holding data from a *previous, unfreed* decode (see runtime.h's
// module docs).
static inline void GameMessage_free(struct GameMessage *value) {
    free((void *)value->player_name);
    value->player_name = NULL;
    Vector3_free(&value->position);
}
