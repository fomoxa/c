// GENERATED BY cyclonec
// DO NOT EDIT MANUALLY
// source: src/models/game.h
// model: GameMessage
// codec: state
// fingerprint: sha256:d0b26077fe6f6fd0cf8a587eb4c906cd1544c18767d9cd5cfeb485833132a12f
// cyclonec-version: 0.2.0
// generated-at: 2026-08-20T13:35:44Z

#pragma once

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>
#include <stdlib.h>
#include <string.h>

#include "runtime.h"
#include "src/models/game.h"
#include "vector3_state.h"

// The "state" codec for GameMessage, generated from its Cyclone markers.
//
// The wire layout, in declaration order (RFC-0002 SS5.1):
//
//  0. `player_id`: `u32`
//  1. `player_name`: `string`
//  2. `position`: `Vector3`
//  3. `health`: `u32`
//  4. `is_alive`: `bool`

static const char *const GameMessageStateCodec_MESSAGE_NAME = "GameMessage.state";
static const uint32_t GameMessageStateCodec_MESSAGE_ID = 0xEB48C20Eu;
static const uint64_t GameMessageStateCodec_FINGERPRINT = 0xD0B26077FE6F6FD0ULL;

// Writes the "state" fields of `*value`, in declaration order. Returns `false` (buffer
// untouched beyond what was already written) if growing the writer's buffer failed.
static inline bool GameMessageStateCodec_encode(CycloneWriter *writer, const struct GameMessage *value) {
    if (!cyclone_writer_write_u32(writer, value->player_id)) return false;
    if (!cyclone_writer_write_string(writer, value->player_name)) return false;
    if (!Vector3StateCodec_encode(writer, &value->position)) return false;
    if (!cyclone_writer_write_u32(writer, value->health)) return false;
    if (!cyclone_writer_write_bool(writer, value->is_alive)) return false;
    return true;
}

// Reads the "state" fields into `*value`, in declaration order.
//
// Fields this codec does not carry are left as they were, which is what lets one
// model be split across several codecs.
//
// A field the stream ended before takes its zero value (RFC-0002 SS9.1); a field
// the stream ended inside is an error. Bytes left over after the last field
// belong to a newer writer's model and are ignored.
//
// `*value` must not already hold data from a previous, unfreed decode - see
// runtime.h's module docs.
static inline CycloneDecodeError GameMessageStateCodec_decode(CycloneReader *reader, struct GameMessage *value) {
    if (cyclone_reader_field_absent(reader)) {
        value->player_id = 0;
    } else {
        CycloneDecodeError error = cyclone_reader_read_u32(reader, &value->player_id);
        if (!cyclone_decode_error_ok(&error)) return error;
    }
    if (cyclone_reader_field_absent(reader)) {
        value->player_name = NULL;
    } else {
        CycloneDecodeError error = cyclone_reader_read_string(reader, &value->player_name);
        if (!cyclone_decode_error_ok(&error)) return error;
    }
    {
        CycloneDecodeError error = Vector3StateCodec_decode(reader, &value->position);
        if (!cyclone_decode_error_ok(&error)) return error;
    }
    if (cyclone_reader_field_absent(reader)) {
        value->health = 0;
    } else {
        CycloneDecodeError error = cyclone_reader_read_u32(reader, &value->health);
        if (!cyclone_decode_error_ok(&error)) return error;
    }
    if (cyclone_reader_field_absent(reader)) {
        value->is_alive = false;
    } else {
        CycloneDecodeError error = cyclone_reader_read_bool(reader, &value->is_alive);
        if (!cyclone_decode_error_ok(&error)) return error;
    }
    return cyclone_decode_ok();
}
