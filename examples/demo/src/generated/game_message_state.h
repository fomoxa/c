// GENERATED BY fomoxac
// DO NOT EDIT MANUALLY
// source: src/models/game.h
// model: GameMessage
// codec: state
// fingerprint: sha256:69961c52b532c9d9e02c8d08bd8eed4eaa99a42b2775629a30c945265c840a75
// fomoxac-version: 0.2.0
// generated-at: 2026-08-23T14:44:47Z

#pragma once

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>
#include <stdlib.h>
#include <string.h>

#include "runtime.h"
#include "src/models/game.h"
#include "vector3_state.h"

// The "state" codec for GameMessage, generated from its Fomoxa markers.
//
// The wire layout, in declaration order (RFC-0002 SS5.1):
//
//  0. `player_id`: `u32`
//  1. `player_name`: `string`
//  2. `position`: `Vector3`
//  3. `health`: `u32`
//  4. `is_alive`: `bool`

static const char *const GameMessageStateCodec_MESSAGE_NAME = "GameMessage.state";
static const uint32_t GameMessageStateCodec_MESSAGE_ID = 0xBC91316Cu;
static const uint64_t GameMessageStateCodec_FINGERPRINT = 0x69961C52B532C9D9ULL;

// Writes the "state" fields of `*value`, in declaration order. Returns `false` (buffer
// untouched beyond what was already written) if growing the writer's buffer failed.
static inline bool GameMessageStateCodec_encode(FomoxaWriter *writer, const struct GameMessage *value) {
    if (!fomoxa_writer_write_u32(writer, value->player_id)) return false;
    if (!fomoxa_writer_write_string(writer, value->player_name)) return false;
    if (!Vector3StateCodec_encode(writer, &value->position)) return false;
    if (!fomoxa_writer_write_u32(writer, value->health)) return false;
    if (!fomoxa_writer_write_bool(writer, value->is_alive)) return false;
    return true;
}

// Reads the "state" fields into `*value`, in declaration order.
//
// Fields this codec does not carry are left as they were, which is what lets one
// model be split across several codecs.
//
// A field the stream ended before takes its zero value (RFC-0002 SS9.1); a field
// the stream ended inside is an error. Bytes left over after the last field
// belong to a newer writer's model and are ignored.
//
// `*value` must not already hold data from a previous, unfreed decode - see
// runtime.h's module docs.
static inline FomoxaDecodeError GameMessageStateCodec_decode(FomoxaReader *reader, struct GameMessage *value) {
    if (fomoxa_reader_field_absent(reader)) {
        value->player_id = 0;
    } else {
        FomoxaDecodeError error = fomoxa_reader_read_u32(reader, &value->player_id);
        if (!fomoxa_decode_error_ok(&error)) return error;
    }
    if (fomoxa_reader_field_absent(reader)) {
        value->player_name = NULL;
    } else {
        FomoxaDecodeError error = fomoxa_reader_read_string(reader, &value->player_name);
        if (!fomoxa_decode_error_ok(&error)) return error;
    }
    {
        FomoxaDecodeError error = Vector3StateCodec_decode(reader, &value->position);
        if (!fomoxa_decode_error_ok(&error)) return error;
    }
    if (fomoxa_reader_field_absent(reader)) {
        value->health = 0;
    } else {
        FomoxaDecodeError error = fomoxa_reader_read_u32(reader, &value->health);
        if (!fomoxa_decode_error_ok(&error)) return error;
    }
    if (fomoxa_reader_field_absent(reader)) {
        value->is_alive = false;
    } else {
        FomoxaDecodeError error = fomoxa_reader_read_bool(reader, &value->is_alive);
        if (!fomoxa_decode_error_ok(&error)) return error;
    }
    return fomoxa_decode_ok();
}
